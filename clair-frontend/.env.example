# Copy to .env.local for local dev, or set as a build-time environment
# variable in your hosting provider (e.g. Vercel project settings).
# Baked in at build time by Vite — see ClairMDEHR.jsx's getApiBase().

VITE_API_BASE=https://your-backend-host.example.com/api
